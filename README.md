# Air Transport Magnate Tracker

To run on dev:

```sh
go run cmd/atm-tracker/main.go
```

To test:

```sh
make test
```

To generate executable:

```sh
make
```

To generate the .app (changes the toml file):

```sh
make package
```
